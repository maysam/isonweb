.panel
  Sensors to plot
  %select.sensor-select{multiple: true}
    -@tags.each do |tag|
      - if cookies[:selected_tags] and cookies[:selected_tags].split.include? tag.uid
        %option{value: tag.uid, selected: true}
          =tag.name
      - else
        %option{value: tag.uid}
          =tag.name
  Plot Type
  %select.plot-type
    -@types.each do |type|
      - if cookies[:plot_type] == type[0]
        %option{value: type[0], selected: true}
          = type[1]
      - else
        %option{value: type[0]}
          = type[1]
  %button#plot_button.btn
    Plot
#graphdiv3.pull-left
#legend.pull-left
.clearfix
%br
(Mouse over to highlight individual values. Click and drag to zoom. Double-click to zoom out.)

